template header
type
gender
lang
overTime

package org.drools.match

import org.drools.match.bean.Info
import org.drools.match.bean.RuleParam



template "overTime"

rule "Over time_@{row.rowNumber}"
    when
        RuleParam(overTime==0)
        info:Info(gender == "@{gender}",lang == "@{lang}")
    then
        // System.out.println(matchTeamInfo);
        System.out.println("测试代码");
end
end template
